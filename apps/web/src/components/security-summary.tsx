import type { ScanResponse } from "@/lib/scan-types";

export function SecuritySummary({ scan }: { scan: ScanResponse }) {
  const risk = scan.summary.critical_finding_count > 0 || scan.summary.high_finding_count > 0 ? "High" : scan.summary.medium_finding_count > 0 ? "Medium" : "Low";
  const metrics = [["Findings", scan.summary.finding_count], ["Critical", scan.summary.critical_finding_count], ["High", scan.summary.high_finding_count], ["Medium", scan.summary.medium_finding_count], ["Low", scan.summary.low_finding_count], ["Routes analyzed", scan.analysis_summary.routes_analyzed], ["Protected", scan.summary.protected_route_count], ["Public", scan.summary.public_route_count], ["Models", scan.summary.prisma_model_count], ["Mapped routes", scan.summary.mapped_route_count]];
  return <section id="review" aria-labelledby="review-title"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="font-mono text-xs uppercase tracking-[.16em] text-emerald-300">Security review</p><h2 className="mt-2 text-3xl font-semibold" id="review-title">{scan.repository.name}</h2></div><span className="rounded-full border border-orange-300/30 bg-orange-300/10 px-3 py-1 font-mono text-xs uppercase tracking-widest text-orange-200">Overall risk: {risk}</span></div><div className="mt-5 grid grid-cols-2 overflow-hidden rounded-xl border border-white/[.08] sm:grid-cols-5">{metrics.map(([label, value]) => <div className="border-b border-r border-white/[.08] bg-[#0c141f] p-4" key={label as string}><p className="font-mono text-[10px] uppercase tracking-wider text-slate-500">{label}</p><p className="mt-2 text-xl font-semibold">{value}</p></div>)}</div><div className="mt-4 flex flex-wrap gap-2"><span className="font-mono text-xs text-slate-500">Technology:</span>{scan.technologies.map((technology) => <span className="rounded border border-white/[.1] px-2 py-1 text-xs text-slate-300" key={technology.name}>{technology.name}</span>)}<span className="ml-2 font-mono text-xs text-slate-500">AI: {scan.ai.status}</span></div></section>;
}
