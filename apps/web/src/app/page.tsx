import { AppShell } from "@/components/app-shell";
import { DemoScanLauncher } from "@/components/demo-scan-launcher";

export default function Home() { return <AppShell><DemoScanLauncher /><section className="border-t border-white/[.08]" id="architecture"><div className="mx-auto max-w-7xl px-5 py-12 lg:px-8"><p className="font-mono text-xs uppercase tracking-wider text-emerald-300">Architecture and trust boundary</p><h2 className="mt-3 text-3xl font-semibold">Evidence first. AI guidance second.</h2><ol className="mt-7 grid gap-3 text-sm sm:grid-cols-2 lg:grid-cols-5">{["Repository", "Deterministic Scanner", "Evidence Package", "AI Reviewer", "Review Guidance"].map((step, index) => <li className="relative rounded-xl border border-white/[.1] bg-[#0c141f] p-4 text-slate-200" key={step}><span className="font-mono text-xs text-emerald-300">{index + 1}</span><span className="mt-2 block font-semibold">{step}</span>{index < 4 ? <span aria-hidden="true" className="absolute -right-3 top-1/2 hidden text-xl text-emerald-300 lg:block">→</span> : null}</li>)}</ol><div className="mt-7 grid gap-5 text-sm leading-6 text-slate-400 md:grid-cols-2"><div><h3 className="font-semibold text-slate-200">Deterministic scanner</h3><p className="mt-2">Sentinel performs deterministic authorization analysis for TypeScript, Express, and Prisma applications: route discovery, authentication discovery, Prisma model discovery, route-to-model mapping, ownership-field analysis, and BOLA/IDOR findings. It is not comprehensive OWASP testing.</p></div><div><h3 className="font-semibold text-slate-200">Evidence-backed AI review</h3><p className="mt-2">AI is disabled without a server-side key. When available, it explains existing evidence only: it never creates or changes deterministic findings. Patches require human review, and the reviewer never executes repository code.</p></div></div><p className="mt-6 text-sm text-slate-500">Public GitHub repositories can be cloned and statically scanned. Scan history, ZIP uploads, exports, automated verification, and deployment are not available.</p></div></section></AppShell>; }
